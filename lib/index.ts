/**
 * # sortedindex
 *
 * - 二分探索を行う関数が入った npm パッケージ
 * - A simple npm package that provides a binary search function for a sorted array
 *
 * ## 使い方 / Usage
 *
 * ```sh
 * npm install sortedindex
 * ```
 * @packageDocumentation
 * @module sortedindex
 */

export { sortedIndex } from "./sortedIndex";
